import { ResortMap } from "./components/ResortMap";

export default function App() {
  return (
    <main
      style={{
        minHeight: "100vh",
        padding: "2rem 1rem",
      }}
    >
      <ResortMap />
    </main>
  );
}
